/**
 * Talk Behavior Interface
 * Each child implements this interface
 * @author Warren Beagle
 */
public interface TalkBehavior {

	/** Talk method to be unique in the children. */
	public String talk();
	
}
